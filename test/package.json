{
  "name": "quarantine-test",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": "^24"
  },
  "scripts": {
    "test": "vitest run",
    "test:contract": "vitest run --project contract --passWithNoTests",
    "test:e2e": "vitest run --project e2e",
    "lint": "biome check",
    "lint:contract": "biome ci --error-on-warnings ./contract",
    "lint:e2e": "biome ci --error-on-warnings ./e2e",
    "lint:ci": "biome ci --error-on-warnings",
    "lint:fix": "biome check --write",
    "lint:fix:unsafe": "biome check --write --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.0",
    "@stoplight/prism-cli": "^5.14.2",
    "adm-zip": "^0.5.16",
    "riteway": "^9.2.0",
    "vitest": "^4.1.2"
  }
}
