---
status: planned
---

# M18: Dashboard Write Processing

## Workflow

1. Read this manifest to understand scope and constraints.
2. Load the [acceptance scenarios](#acceptance-scenarios) for the current command.
3. Use `/mikey:tdd <scenario-file>` to implement against those scenarios.
4. After implementation, verify against [acceptance criteria](#acceptance-criteria)
   and [flow verification](#verification).
5. Use `/mikey:testify` to validate test quality.

## Scope

**MUST implement:**
- Extend the existing artifact processing pipeline: after ingesting test results into SQLite, also perform GitHub writes using the dashboard's App installation tokens
- **State branch creation**: if state branch does not exist on first artifact processing, create it with initial empty state (R-6)
- **State update**: read current state from state branch, merge new flaky tests (quarantine-wins semantics per ADR-012), write via CAS with maximum 3 retries on 409 conflict (exponential backoff with jitter)
- **Issue creation**: for each new flaky test, check dedup via Search API (`quarantine:<suite>:<hash>` label), create issue with title/body/labels matching CLI v1 format. Respect `issue_skipped_reason` field (ADR-022): skip issue creation for `"new_file_in_pr"` and `"new_test_in_pr"`.
- **Unquarantine**: batch check closed issues via Search API **during artifact processing** (as part of state merge, before writing state). Remove tests whose tracking issues are closed.
- **PR comment**: post or update quarantine summary comment on the PR using suite-specific marker (`<!-- quarantine:<suite-name> -->`). Skip entirely when `pr_number` is null (non-PR build). In v2 mode, append a note: `"Note: Quarantine changes take effect on the next CI run, not this one."` (stale-state communication)
- **cli_mode gate**: when `cli_mode` is `"v1"`, skip all writes (CLI already performed them)
- **SEC-3**: App permission parity verified at startup; log warning for excess permissions
- **SEC-10**: Sanitize `failure_message`, `name`, and `test_id` fields before rendering into GitHub markdown (escape HTML/markdown injection). All three fields are rendered into issue titles, bodies, or labels.
- **SEC-11**: Cross-check artifact `repo` field against the `owner/repo` the artifact was downloaded from; reject mismatches with warning
- **Resource bounds**: reject artifacts with >100,000 test entries; truncate string fields exceeding 64KB before rendering
- Results JSON schema validation: ensure required fields present and sufficient for rendering
- Validation ordering: schema validation (required fields, types) MUST run before the SEC-11 repo cross-check, so a missing `repo` field is caught as a schema error rather than a cross-check failure
- Write error handling: all writes are best-effort; failures logged as warnings, never crash the processing pipeline
- Write processing rate limiting: not required for v2 (polling-based ingestion naturally throttles). Revisit when webhook-triggered processing is added in v3.
- Rendering parity contract tests: issue body and PR comment rendering verified against shared fixtures. **Byte-for-byte identical** to CLI v1 output (excluding trailing whitespace). Shared fixtures in `testdata/`.

**MUST NOT implement:**
- CLI code changes (M17)
- E2E round-trip testing (M19)
- Webhook-triggered processing (v3 -- see ADR-027)
- `POST /api/ci-token` endpoint (retired by ADR-036 amendment)

## Acceptance criteria

From [plan: adr-036-token-proxy.md#m18-dashboard-write-processing](../plans/adr-036-token-proxy.md#m18-dashboard-write-processing):

1. Dashboard processes a quarantine artifact and writes updated state to the state branch via CAS with maximum 3 retries (R-4)
2. Dashboard creates state branch with initial empty state if absent on first artifact processing (R-6)
3. Dashboard creates issues for new flaky tests with correct title, body, labels matching CLI v1 format (R-4)
4. Dashboard dedup: no duplicate issue created when one already exists with the same `quarantine:<suite>:<hash>` label (R-4)
5. Dashboard respects `issue_skipped_reason`: tests with `"new_file_in_pr"` or `"new_test_in_pr"` skip issue creation (R-4, ADR-022)
6. Dashboard posts/updates PR comment with quarantine summary using suite-specific marker (R-4)
7. When `pr_number` is null (non-PR build), PR comment step is skipped entirely (R-4)
8. Dashboard uses suite-specific markers and labels per ADR-032 (R-4)
9. Results JSON missing required fields -> artifact skipped with warning (R-5)
10. When `cli_mode` is `"v1"`, dashboard skips all writes (R-5)
11. State CAS conflict -> retry with merge (quarantine wins), max 3 retries, exponential backoff with jitter (R-4)
12. Issue creation failure -> logged as warning, state still updated (R-4)
13. PR comment failure -> logged as warning, does not affect state or issues (R-4)
14. Closed-issue check (during artifact processing): quarantined tests with closed tracking issues are removed from state (R-4)
15. `failure_message`, `name`, and `test_id` fields sanitized before markdown rendering -- no HTML/markdown injection possible (SEC-10)
16. Artifact `repo` field cross-checked against download source repo; mismatch -> artifact rejected with warning (SEC-11)
17. Artifacts with >100,000 test entries rejected; string fields exceeding 64KB truncated before rendering (resource bounds)
18. App permissions beyond SEC-3 table logged as warning at startup; verified via `GET /app` API call comparing configured vs required permissions (SEC-3)
19. Issue body and PR comment output matches CLI v1 rendering byte-for-byte (excluding trailing whitespace) for the same input data (contract test) (R-4)
20. `make dash-test && make dash-lint && make dash-typecheck` pass

Requirements: [ADR-036 amendment](../adr/036-v2-cli-auth-github-token-default.md#amendment-2026-04-20-server-side-writes-replace-token-proxy).

## Specifications

Load these on demand -- do not read all upfront:

| What | Reference |
|------|-----------|
| ADR-036 amendment (server-side writes) | [adr/036-v2-cli-auth-github-token-default.md#amendment-2026-04-20-server-side-writes-replace-token-proxy](../adr/036-v2-cli-auth-github-token-default.md#amendment-2026-04-20-server-side-writes-replace-token-proxy) |
| State merge semantics (CLI reference impl) | `cli/internal/quarantine/state.go` |
| Issue body rendering (CLI reference impl) | `cli/cmd/quarantine/run_notifications.go` (lines 102-162) |
| PR comment rendering (CLI reference impl) | `cli/cmd/quarantine/run_notifications.go` (lines 193-285) |
| CAS write logic (CLI reference impl) | `cli/internal/cas/cas.go` |
| Issue dedup (CLI reference impl) | `cli/internal/github/issues_ops.go` (lines 36-115) |
| Issue skip / new-to-PR detection | `cli/cmd/quarantine/run_notifications.go` (issue_skipped_reason) |
| Results JSON schema | `schemas/test-result.schema.json` |
| State schema | `schemas/quarantine-state.schema.json` |
| Per-suite isolation | [ADR-032](../adr/032-per-suite-state-isolation.md) |
| Quarantine-wins merge | [ADR-012](../adr/012-quarantine-wins-on-conflict.md) |
| Issue skip (new-to-PR) | [ADR-022](../adr/022-issue-creation-scope.md) |
| App permission parity | SEC-3 in [v2-auth-token-proxy.md](../specs/v2-auth-token-proxy.md) |
| Existing artifact processing | `dashboard/app/lib/sync.server.ts`, `dashboard/app/lib/ingest.server.ts` |
| Installation token provider | `dashboard/app/lib/installation-token.server.ts` |

## Acceptance scenarios

**Scenarios must be created before implementation.** Run `/create-user-scenario` to generate `docs/scenarios/v2/04-dashboard-write-processing.md` from the plan's scenario outlines.

Use these with `/mikey:tdd`:

| Scenarios | File | Topic |
|-----------|------|-------|
| TBD | [v2/04-dashboard-write-processing.md](../scenarios/v2/04-dashboard-write-processing.md) | State write, state branch creation, issue creation + dedup + skip, PR comment, unquarantine, cli_mode gate, validation, security |

## Verification

**Write processing pipeline:** After artifact ingestion, the dashboard MUST:
1. Validate artifact: schema validation first (required fields, types), then repo cross-check (SEC-11), then resource bounds (100K tests, 64KB strings)
2. Check `cli_mode` -- if `"v1"`, skip all writes
3. Read current state from `.quarantine/<suite>/state.json` on the state branch (or create branch if absent)
4. Batch check closed issues via Search API (unquarantine step)
5. Merge new flaky tests into state (quarantine wins on conflict)
6. Write updated state via CAS (max 3 retries, exponential backoff with jitter)
7. For each new flaky test: check `issue_skipped_reason`, search for existing issue, create if not found
8. If `pr_number` is present: post or update PR comment

**Rendering parity:** Issue bodies and PR comment output MUST be byte-for-byte
identical to CLI v1 rendering for the same input data (excluding trailing
whitespace). Verify via contract tests using shared fixtures from `testdata/`.

**Error isolation:** Each write operation MUST be independent:
- State update failure -> log warning, continue to issue creation
- Issue creation failure -> log warning, continue to PR comment
- PR comment failure -> log warning, pipeline completes

**Security invariants:**
- `failure_message`, `name`, and `test_id` fields MUST be sanitized before markdown rendering
- Artifact `repo` field MUST match the download source repo
- Artifacts with >100,000 test entries MUST be rejected

**Build:** `make dash-test && make dash-lint && make dash-typecheck` MUST pass.
